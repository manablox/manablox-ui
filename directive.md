We want a clone of PrimeVue. The repository can be found here: https://github.com/primefaces/primevue/tree/master

The goal is to strip out any Vue dependency and to have every component implemented as plain webcomponent with all the features the primevue components provide.

use pnpm as package manager, global css vars for the theme.
